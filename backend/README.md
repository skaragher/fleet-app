# Fleet Backend (Node.js + Express + Prisma)

## Démarrage rapide
```bash
cd backend
cp .env.example .env
npm install
npm run prisma:generate
npm run prisma:migrate
npm run seed
npm run dev
```

- API: http://localhost:3000
- Login demo:
  - email: admin@demo.local
  - password: admin123
